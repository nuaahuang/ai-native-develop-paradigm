
---

# AI-Native 极小粒度闭环开发范式：完整方案报告

**版本**：v1.0  
**适用场景**：常规迭代开发（1-3 天），基于已有产品的增量更新  
**核心理念**：人定意图，AI 完成工程化闭环；极小粒度任务，每任务即验收；持续维护功能全貌

---

## 第一部分：范式总览

### 1.1 核心原则

| 原则 | 说明 |
|------|------|
| 极小粒度任务 | 每个 Task 是一个原子级功能点，输入输出明确，逻辑闭环 |
| 每任务即验收 | 每个 Task 完成后立即触发自动测试，保证质量内建 |
| 迭代隔离 | 每个迭代的产物独立保存，可追溯、可回滚、可对比 |
| 增量更新 | 基于已有代码和设计做修改，不重写 |
| 功能全貌 | 维护持续更新的 Product Map，AI 始终知道产品完整形态 |
| 人机边界 | 人 = 意图 + 决策 + 例外处理；AI = 理解 + 分解 + 执行 + 验证 + 报告 |

### 1.2 目标指标

| 指标 | 当前 | 目标 |
|------|------|------|
| 40 人日项目周期 | 8-12 工作日 | 1-3 工作日 |
| 人工投入占比 | ~60% | ≤ 20% |
| 测试通过率 | 人工回归 | ≥ 95%（AI 测试） |
| 验收时间 | 1-2 天 | ≤ 2 小时 |
| 文档质量 | 人工撰写 | AI 生成 + 人确认 |

---

## 第二部分：Product Map —— 功能全貌基座

Product Map 是贯穿所有迭代的“完整认知基座”，每次迭代结束后由 AI 更新，人确认。它解决的是“迭代间信息不完整，不知道功能全貌”的问题。

### 2.1 三层结构

```
Product Map
├── Layer 1: 用户旅程层（journeys.yaml）
│   └── 用户视角的完整流程，如：登录 → 浏览 → 下单 → 支付
├── Layer 2: 功能页面层（pages.yaml）
│   └── 每个页面的完整定义：UI 结构 + 交互路径 + 数据依赖 + 变更历史
└── Layer 3: 能力网络层（capabilities.yaml）
    └── 接口 / 数据实体 / 业务规则的网状关系
```

### 2.2 存储位置

```
project/
├── product-map/
│   ├── journeys.yaml
│   ├── pages.yaml          ← 最核心，每个页面完整定义
│   ├── capabilities.yaml
│   └── index.yaml          ← 版本索引
├── iterations/             ← 每个迭代的增量产物
│   ├── v1.0/
│   └── v1.1/
└── src/
```

### 2.3 更新机制

- **更新时机**：每个迭代的 Phase 6（交付与验收）中
- **更新方式**：AI 读取本次迭代所有产物，对比当前 Product Map，输出更新 diff
- **确认方式**：人确认 diff 无误后，AI 写入更新
- **变更追溯**：每个页面/接口/实体都有 `changeLog`，记录历次变更

---

## 第三部分：完整开发流程（六大阶段）

### Phase 0：初始化与输入

**输入**：PRD + UI 图（截图/Figma）+ 代码仓库  
**动作**：
- 人确认技术栈、项目上下文
- AI 读取 Product Map（功能全貌）和上一迭代产物

**产出**：项目上下文就绪

---

### Phase 1：需求理解与极细粒度拆解

**输入**：PRD + UI 图 + Product Map + 上一迭代产物  
**输出**：
- 变更清单（新增/修改/删除，包含变更前后对比）
- UseCase 列表
- UI Element Tree（页面 → 区块 → 字段）
- 需求分解 DAG（Task 依赖关系图，每个 Task 为原子级功能点）
- **变更前后对比表**（原有状态 vs 当前状态）
- Ambiguity Checklist（待澄清项，AI 主动提问）

**关键动作**：
1. **Product Map 完整性校验**：AI 自动校验 Product Map 的完整性和一致性
2. **变更分析**：AI 对比 PRD + UI 图与 Product Map，输出变更清单
3. **变更前后对比**：AI 生成"原有状态 vs 当前状态"对比表，明确每个变更点的前后差异
4. **需求拆解**：AI 将变更拆解为极小粒度 Task
5. **主动澄清**：AI 输出模糊点清单，人回答（是/否/选择题）

**人介入点**：
- 确认变更清单（包含变更前后对比）
- 确认 Product Map 完整性校验结果
- 回答澄清问题（10-20 分钟）

**需求明确定义要求**：
- 每个变更项必须包含：变更前状态、变更后状态、变更原因
- 涉及字段变更的，必须明确字段来源（UI/业务规则）
- 涉及业务规则变更的，必须明确规则变更内容

---

### Phase 2a：领域建模 + 表结构设计

**输入**：确认后的 UseCase + UI Element Tree + Product Map + 现有 DB Schema  
**输出**：
- 领域模型（Entity / Aggregate / ValueObject / Enum）
- 物理表结构设计（Migration / Schema 文件）
- 字段溯源注释（每个字段标注来自 UI 的哪个位置）
- 模型自检报告（3NF 检查、UI 字段 100% 覆盖）

**关键动作**：
1. AI 读取现有表结构（Product Map 中）
2. AI 根据变更清单，增量设计表结构
   - 新增：追加新表/新字段
   - 修改：在原有表上增减字段，标注变更标记
   - 删除：标记废弃，不物理删除
3. AI 输出自检报告

**人介入点**：确认领域模型 + 表结构（10-15 分钟）

---

### Phase 2b：接口设计 + UI↔API 映射

**输入**：确认后的表结构 + UI Element Tree + Product Map  
**输出**：
- OpenAPI 3.1 规范（增量更新，保留 changelog）
- UI↔API 字段映射表（HTML 格式，截图 + 红框标注 + 字段来源）
- Mock 数据

**关键动作**：
1. AI 读取现有 OpenAPI（Product Map 中）
2. AI 根据变更清单，增量设计接口
   - 新增接口：追加
   - 修改接口：更新 + 追加 changelog
   - 删除接口：标记 deprecated
3. AI 生成 UI↔API 映射 HTML（人友好，可交互）

**人介入点**：无需介入（除非特殊业务逻辑需确认）

---

### Phase 3：前端 UI 与接口契约对齐

**输入**：UI Element Tree + OpenAPI + UI↔API 映射 HTML  
**输出**：
- 前端 Task 清单（每个 Task 含：组件路径、数据源、渲染逻辑）
- 增量 Task DAG（绿色=新增，黄色=修改，红色=删除）

**关键动作**：
1. AI 读取前端现有代码结构
2. AI 根据映射表和变更清单，生成前端 Task
3. AI 输出 Task DAG，标注变更类型

**人介入点**：无需介入

---

### Phase 4：连续批处理编码与即时验收

采用**连续批处理（Continuous Batch Processing）**思想，后端按批次输出接口，前端接收一批处理一批，实现前后端流水线式并行协作。

#### Step 4.1：后端批处理开发

**输入**：后端 Task DAG + 表结构 + OpenAPI  
**输出**：
- Model / Repository / Service / Controller 代码（分批输出）
- 单元测试（正常 + 异常 + 边界）
- 桩模块（分批更新）
- 批次验收状态（Batch Pass/Fail）

**关键动作**：
1. AI 按 DAG 依赖分组，并行处理后端 Task
2. 每批接口完成后 → 触发单元测试 → 验收通过标记 → 输出可用接口批次
3. 批次粒度：按业务域或页面维度划分，每批包含 3-5 个关联接口

#### Step 4.2：前端连续消费开发

**输入**：前端 Task DAG + UI↔API 映射 + 后端桩模块 + 分批接口  
**输出**：
- 页面组件（分批完成）
- API 调用封装（分批生成）
- 数据绑定
- 组件测试
- 每个 Task 的验收状态（Pass/Fail）

**关键动作**：
1. 无需等待后端全部完成，采用**事件驱动触发**
2. 后端输出一批接口 → 自动触发前端对应批次的开发
3. 前端优先使用 Mock 数据开发，后端接口就绪后自动切换为真实接口
4. 每个 Task 完成后 → 触发组件测试 + Mock/真实联调测试 → 验收通过标记

**人介入点**：可选 Review（0-15 分钟）

#### Step 4.3：批次协调机制

| 协调机制 | 说明 |
|----------|------|
| 批次依赖图 | 定义接口批次间的依赖关系，确保上游批次就绪后下游才开始 |
| 就绪通知 | 后端批次验收通过后，自动通知前端消费 |
| Mock 降级 | 后端未就绪时，自动使用 Mock 数据支撑前端开发 |
| 接口切换 | 后端接口就绪后，自动从 Mock 切换为真实接口 |

---

### Phase 5：自动化测试与 AI 验收

**输入**：所有 Task 状态 + 测试报告  
**输出**：
- 增量测试报告（新增测试 + 回归测试）
- 接口契约测试报告
- UI 字段覆盖率报告
- 异常场景测试集

**关键动作**：
1. AI 只对变更涉及的 Task 重新生成和运行测试
2. AI 运行所有已有回归测试
3. AI 输出测试报告

**人介入点**：查看测试报告（5 分钟）

---

### Phase 6：交付与验收

**输出物清单**：

| 交付物 | 格式 | 用途 |
|--------|------|------|
| 变更清单式验收报告 | Markdown | PM / 技术负责人 |
| 增量接口文档 | OpenAPI + HTML | 前后端对接 |
| UI↔API 映射表（更新版） | HTML | 前端 / QA |
| 变更影响说明 | Markdown | 运维 / 测试 |
| 自动化测试报告 | HTML | 质量确认 |
| **更新后的 Product Map** | YAML | **下次迭代的基座** |

**验收清单示例**：
```
## v1.1 变更验收报告

### 新增功能（3项）
✅ 订单列表页 → 接口 / 页面 / 测试 全部通过

### 修改功能（2项）
✅ 订单详情页增加物流信息 → 接口 / 页面 / 测试 全部通过

### 废弃功能（1项）
⚠️ old_shipping_method 已标记废弃，v2.0 移除

### 回归测试
✅ 全部 47 项已有测试通过
```

**关键动作**：
1. AI 生成验收报告
2. **AI 更新 Product Map**（追加/修改页面、接口、实体的定义 + 变更历史）
3. 人确认 Product Map 更新
4. 人主流程走查 + 签字确认

**人介入点**：确认 Product Map 更新 + 主流程验收（30-60 分钟）

---

## 第四部分：迭代隔离与增量更新机制

### 4.1 迭代目录结构

```
project/
├── iterations/
│   ├── v1.0/
│   │   ├── spec/          # Phase 1 产物
│   │   ├── design/        # Phase 2 产物
│   │   ├── mapping/       # Phase 3 产物
│   │   ├── tasks/         # Phase 4 产物
│   │   └── verify/        # Phase 5-6 产物
│   ├── v1.1/
│   └── v2.0/
├── product-map/           # 功能全貌（持续更新）
└── src/                   # 代码（始终最新）
```

### 4.2 增量更新策略

| 阶段 | 增量做法 |
|------|----------|
| 变更分析 | 对比 Product Map + PRD，输出变更清单 |
| 表结构 | 追加新表/字段，修改已有字段，标记废弃 |
| 接口 | 追加新接口，修改已有接口（保留 changelog），标记 deprecated |
| UI 映射 | 在原 HTML 上追加/修改标注 |
| Task DAG | 在上一个 DAG 基础上追加/修改节点 |
| 编码 | 只执行新增和修改的 Task |
| 测试 | 新增测试 + 回归测试 |
| 验收报告 | 只报告变更部分 + 回归结果 |

### 4.3 迭代上下文传递

每次迭代开始时，AI 自动获取：
1. `product-map/` → 产品完整功能全貌
2. `iterations/previous/` → 上一迭代的增量产物
3. 本次 PRD + UI 图 → 本次变更目标

AI 基于这三者，输出变更清单，经人确认后进入开发。

---

## 第五部分：人与 AI-Agent 的边界定义

### 5.1 人负责（不可替代）

| 阶段 | 人的职责 | 预计耗时 |
|------|----------|----------|
| 输入 | 提供 PRD + UI 图 + 确认项目上下文 | 准备阶段 |
| Phase 1 | 确认变更清单 + 回答澄清问题 | 10-20 min |
| Phase 2a | 确认领域模型 + 表结构 | 10-15 min |
| Phase 4 | 可选 Review 关键文件 | 0-15 min |
| Phase 6 | 确认 Product Map 更新 + 主流程验收 | 30-60 min |

### 5.2 AI-Agent 负责（完全自动化）

| 阶段 | AI 职责 |
|------|---------|
| Phase 1 | PRD 解析、UI 解析、变更分析、需求分解、主动澄清 |
| Phase 2a | 模型提议、表结构设计、自检、字段溯源 |
| Phase 2b | 接口设计、UI↔API 映射、Mock 生成 |
| Phase 3 | 前端 Task 拆解、DAG 生成 |
| Phase 4 | 并行编码、单元测试、桩模块、每 Task 验收 |
| Phase 5 | 自动化测试、回归测试、覆盖率检查 |
| Phase 6 | 验收报告生成、Product Map 更新、文档整理 |

---

## 第六部分：技术路线与工具选型

### 6.1 落地形态

本范式不依赖独立平台，而是以 **Project Rules + Skills + 可选 MCP Server** 的形式，运行在现有的 AI 编码工具中：

| 层次 | 内容 | 工具支持 |
|------|------|----------|
| Layer 1: Project Rules | 范式定义、编码规范、角色设定 | `.cursorrules` / `CLAUDE.md` |
| Layer 2: Skills | 每个阶段对应一个 Skill 指令 | `SKILL.md`（Cursor/Trae/Claude Code 原生支持） |
| Layer 3: MCP Server | 外部能力：读 DB、存制品、校验 | 可选，Python/TS 实现 |

### 6.2 可插拔架构设计

为确保每个流程阶段和 Skill 都能**独立使用、灵活组合**，采用以下可插拔设计原则：

#### 6.2.1 独立使用原则

| 原则 | 说明 |
|------|------|
| 输入输出标准化 | 每个 Skill 定义明确的输入输出格式，不依赖内部状态 |
| 无状态设计 | Skill 执行不依赖前序 Skill 的执行上下文 |
| 自包含性 | 每个 Skill 自带必要的配置和默认值 |
| 渐进增强 | 支持从简单到复杂的使用场景 |

#### 6.2.2 Skill 接口规范

每个 Skill 遵循统一的接口规范，确保可独立调用：

| Skill 名称 | 输入规范 | 输出规范 | 独立使用场景 |
|-----------|----------|----------|--------------|
| `spec` | PRD 文本/UI 图片/Product Map（可选） | 变更清单 + UseCase + Task DAG | 单独进行需求分析 |
| `design-domain` | UseCase + UI Element Tree（可选：Product Map） | 领域模型 + 表结构设计 + 自检报告 | 单独进行领域建模 |
| `design-api` | 表结构 + UI Element Tree（可选：Product Map） | OpenAPI + UI↔API 映射 + Mock 数据 | 单独进行接口设计 |
| `align-frontend` | OpenAPI + UI↔API 映射 | 前端 Task 清单 + DAG | 单独进行前端任务拆解 |
| `impl-backend` | OpenAPI + 表结构（可选：Task DAG） | 后端代码 + 单元测试 + 桩模块 | 单独进行后端开发 |
| `impl-frontend` | OpenAPI + UI↔API 映射（可选：桩模块） | 前端组件 + API 封装 + 测试 | 单独进行前端开发 |
| `verify` | 代码仓库 + 测试配置 | 测试报告 + 覆盖率报告 + 验收报告 | 单独进行测试验收 |

#### 6.2.3 Skill 组合机制

支持多种组合方式，满足不同场景需求：

| 组合方式 | 说明 | 适用场景 |
|----------|------|----------|
| **全链路执行** | 按顺序执行所有 Skill | 完整迭代开发 |
| **阶段组合** | 选择连续的几个阶段执行 | 部分迭代（如仅设计阶段） |
| **单点调用** | 仅执行单个 Skill | 临时任务（如单独生成 Mock） |
| **条件分支** | 根据前序结果决定是否执行后续 Skill | 灵活流程控制 |
| **循环调用** | 对多个输入批量执行同一 Skill | 批量处理 |

#### 6.2.4 独立使用示例

```
# 场景1：仅进行需求分析
/spec --prd="需求文档.md" --ui="设计稿.png"

# 场景2：仅生成接口文档
/design-api --schema="schema.json" --elements="ui-elements.json"

# 场景3：仅进行后端开发
/impl-be --openapi="openapi.yaml" --schema="schema.sql"

# 场景4：仅执行测试验证
/verify --repo="./src" --test-config="jest.config.js"
```

### 6.3 Skill 清单

| Skill 名称 | 对应阶段 | 触发方式 |
|-----------|----------|----------|
| `spec` | Phase 1 | `/spec` |
| `design-domain` | Phase 2a | `/design-domain` |
| `design-api` | Phase 2b | `/design-api` |
| `align-frontend` | Phase 3 | `/align` |
| `impl-backend` | Phase 4.1 | `/impl-be` |
| `impl-frontend` | Phase 4.2 | `/impl-fe` |
| `verify` | Phase 5-6 | `/verify` |

### 6.4 推荐技术栈

| 能力 | 推荐 |
|------|------|
| Agent 编排 | LangGraph / AutoGen / 原生 Skill 链 |
| UI 解析 | GPT-4o / Claude 3.7 Vision |
| PRD 解析 | LLM + Prompt Chain |
| 代码理解 | AST Parser + Repo Map |
| 代码生成 | LLM + 模板引擎 |
| 测试生成 | LLM + 规则引擎 |
| 文档生成 | LLM + 模板（OpenAPI / Markdown / HTML）|
| MCP Server | Python `mcp` SDK / TS `@modelcontextprotocol/sdk` |

---

## 第七部分：成功标准

### 7.1 量化指标

| 指标 | 当前 | 目标 |
|------|------|------|
| 40 人日项目周期 | 8-12 工作日 | 1-3 工作日 |
| 人工投入占比 | ~60% | ≤ 20% |
| 测试通过率 | 人工回归 | ≥ 95%（AI 测试） |
| 验收时间 | 1-2 天 | ≤ 2 小时 |
| Product Map 准确性 | 无 | ≥ 95%（人确认） |

### 7.2 质化指标

- 人只需关注“做什么”，无需关注“怎么做”
- 任何中间产物都可追溯（需求 → 模型 → 接口 → 代码 → 测试）
- 新成员可在 1 天内上手该范式
- 可复用到不同项目和技术栈
- 产品功能全貌始终完整、可查询

---

## 第八部分：风险与应对

| 风险 | 应对策略 |
|------|----------|
| LLM 幻觉导致设计错误 | 增加自检规则 + 人确认节点 |
| UI 解析精度不足 | 混合方案：VLM + 结构化标注 |
| Product Map 维护不及时 | 固化到 Phase 6 的强制步骤 |
| 增量更新遗漏 | 变更清单 + 人确认双重保障 |
| 复杂业务逻辑遗漏 | 主动澄清机制 + UseCase 全覆盖检查 |
| Agent 间死锁 | DAG 调度 + 超时回退 |

---

## 第九部分：总结

这个范式的本质是：

**将“需求 → 设计 → 编码 → 测试 → 交付”的全链路，从人的脑力劳动转变为 AI-Agent 的可编排流水线。**

三个关键创新点：

1. **极小粒度任务 + 每任务即验收** → 质量内建，不堆积到最后
2. **迭代隔离 + 增量更新** → 可持续演进，不重写
3. **Product Map 功能全貌** → AI 始终知道产品完整形态，迭代间信息不丢失

当这个范式跑通，一个 40 人日的项目变成 1-3 天，且质量可控、可追溯、可复现——这不再是效率提升，而是开发模式的代际跃迁。

---

