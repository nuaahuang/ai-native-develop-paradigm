# design-api Skill 详细设计方案

## 1. 概述

`design-api` Skill 负责**接口设计 + UI↔API 映射**，是 AI-Native 开发范式中的核心设计阶段（对应 Phase 2b）。该 Skill 可独立使用，特别适合快速生成接口文档的场景。

**核心使用场景**：写完接口后，上传 UI 截图和接口信息，Skill 自动生成带截图标注的接口文档（MD 格式），支持增量更新，方便拷贝迁移。

## 2. 功能定位

| 维度 | 说明 |
|------|------|
| **阶段定位** | Phase 2b：接口设计 + UI↔API 映射 |
| **核心职责** | 根据表结构和 UI Element Tree 设计接口，生成 OpenAPI 规范和字段映射文档 |
| **输入来源** | 表结构设计、UI Element Tree、Product Map（可选）、UI 截图（可选） |
| **输出产物** | OpenAPI 规范、UI↔API 映射表（MD/HTML）、Mock 数据、接口文档 |

## 3. 输入规范

### 3.1 输入参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `schema` | SQL/JSON | 是 | 数据库表结构（SQL DDL 或 JSON 格式） |
| `ui_elements` | JSON | 是 | UI Element Tree（页面→区块→字段结构） |
| `ui_screenshot` | Image/URL | 否 | UI 截图（带标注的设计图） |
| `product_map` | JSON/YAML | 否 | 现有 Product Map，用于增量设计 |
| `api_base_path` | String | 否 | API 基础路径（默认：`/api`） |
| `output_format` | String | 否 | 输出格式：markdown（推荐）/ html（默认：markdown） |
| `output_file` | String | 否 | 输出文件路径（支持增量更新） |
| `api_info` | JSON | 否 | 接口元信息（名称、描述、认证方式等） |

### 3.2 输入格式示例

#### 表结构输入（SQL DDL）
```sql
CREATE TABLE `{表名}` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '{主键说明}',
  `{字段名1}` {类型} COMMENT '{字段说明}',
  `{字段名2}` {类型} COMMENT '{字段说明}',
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='{表说明}';
```

#### UI Element Tree 输入（JSON）
```json
{
  "pages": [
    {
      "pageName": "{页面名称}",
      "blocks": [
        {
          "blockName": "{区块名称}",
          "fields": [
            {"name": "{字段名}", "label": "{标签}", "type": "{类型}", "annotation": "{标注号}"}
          ]
        }
      ]
    }
  ]
}
```

#### API 元信息输入（JSON）
```json
{
  "apiName": "{接口名称}",
  "apiDescription": "{接口描述}",
  "httpMethod": "GET/POST/PUT/DELETE",
  "path": "/{路径}/{参数}",
  "authType": "{认证方式}",
  "version": "{版本}",
  "tags": ["{标签1}", "{标签2}"]
}
```

## 4. 输出规范

### 4.1 输出产物清单

| 产物 | 格式 | 用途 |
|------|------|------|
| **OpenAPI 规范** | YAML | 标准 OpenAPI 3.1 格式接口文档 |
| **UI↔API 映射表** | Markdown（推荐） | 截图标注与字段的映射关系，方便拷贝迁移 |
| **接口文档** | Markdown | 完整的接口说明文档 |
| **Mock 数据** | JSON | 模拟响应数据 |
| **变更清单** | Markdown | 相对于现有文档的增量变更 |

### 4.2 输出格式示例

#### UI↔API 映射表输出（Markdown）
```markdown
# 接口文档：{接口名称}

---

## 一、接口信息

| 项目 | 值 |
|------|-----|
| **接口名称** | {接口名称} |
| **接口路径** | `{HTTP方法} {路径}` |
| **认证方式** | {认证方式} |
| **版本** | {版本} |
| **所属模块** | {模块名称} |

---

## 二、UI 截图与字段映射

![{图片描述}]({图片路径})

### 字段映射说明

| 标注号 | UI 位置 | 字段名 | 接口字段 | 类型 | 说明 |
|--------|----------|--------|----------|------|------|
| {标注号} | {UI位置} | {字段名} | {接口字段} | {类型} | {说明} |

---

## 三、请求参数

### 路径参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| {参数名} | {类型} | {是/否} | {说明} |

### 查询参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|

---

## 四、响应结构

### 成功响应（200 OK）

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "{字段名}": "{值}"
  }
}
```

### 字段说明

| 字段名 | 类型 | 说明 |
|--------|------|------|
| {字段名} | {类型} | {说明} |

---

## 五、错误响应

| HTTP 状态码 | 错误码 | 错误消息 | 说明 |
|-------------|--------|----------|------|
| 400 | {错误码} | "{错误消息}" | 参数校验失败 |
| 401 | {错误码} | "{错误消息}" | 未授权 |
| 404 | {错误码} | "{错误消息}" | 资源不存在 |
| 500 | {错误码} | "{错误消息}" | 系统内部错误 |

---

## 六、业务规则说明

1. **{规则名称}**：
   - {规则说明}

2. **{规则名称}**：
   - {规则说明}

---

## 七、变更历史

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| {版本} | {日期} | {变更内容} |

---

*Generated by design-api Skill*
```

#### OpenAPI 输出（YAML）
```yaml
openapi: 3.1.0
info:
  title: {接口名称}
  description: {接口描述}
  version: {版本}
servers:
  - url: {基础路径}
paths:
  {路径}:
    {HTTP方法}:
      summary: {接口名称}
      description: {接口描述}
      parameters:
        - name: {参数名}
          in: path
          required: true
          schema:
            type: {类型}
      responses:
        '200':
          description: 成功响应
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      {字段名}:
                        type: {类型}
```

#### Mock 数据输出（JSON）
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "{字段名1}": "{mock值1}",
    "{字段名2}": {mock值2},
    "{字段名3}": ["{mock值}"]
  }
}
```

## 5. 核心处理流程

```
输入解析 → 接口设计 → UI↔API 映射 → 文档生成 → 增量更新处理 → 输出
```

### 5.1 输入解析
- 解析表结构（支持 SQL DDL 和 JSON 格式）
- 解析 UI Element Tree
- 解析 API 元信息（如有）

### 5.2 接口设计
- 根据表结构生成基础接口
- 根据业务规则添加参数校验
- 设计响应结构

### 5.3 UI↔API 映射
- 建立 UI 标注与接口字段的映射关系
- 生成字段溯源信息

### 5.4 文档生成
- 生成 Markdown 格式接口文档
- 生成 OpenAPI YAML 规范
- 生成 Mock 数据

### 5.5 增量更新处理
- 读取目标文档（如有）
- 判断接口是否已存在
- 新增：追加到文档末尾
- 更新：替换原有内容，保留变更历史
- 删除：标记 deprecated，保留文档

## 6. 核心规则

### 6.1 接口设计规则
- **RESTful 规范**：使用标准 HTTP 方法（GET/POST/PUT/DELETE）
- **命名规范**：路径使用 kebab-case，字段使用 camelCase
- **状态码**：200 成功、400 参数错误、401 未授权、404 不存在、500 服务器错误

### 6.2 映射规则
- **字段匹配**：根据名称和类型进行智能匹配
- **标注溯源**：每个字段关联到 UI 截图的标注号
- **变更标记**：新增字段标记为绿色，修改字段标记为黄色

### 6.3 输出规则
- **Markdown 优先**：默认输出 MD 格式，便于拷贝迁移
- **结构统一**：所有接口文档使用统一模板
- **可追溯性**：包含字段溯源和变更历史

## 7. 调用方式

### 7.1 命令行调用
```bash
design-api \
  --schema="schema.sql" \
  --ui_elements="ui_elements.json" \
  --output="./output" \
  --output_format="markdown"
```

### 7.2 API 调用
```bash
POST /api/design-api
Content-Type: application/json

{
  "schema": "...",
  "ui_elements": {...},
  "output_format": "markdown"
}
```

### 7.3 Skill 调用
```
/design-api --schema="schema.sql" --ui_elements="ui_elements.json" --output="./docs/api.md"
```

## 8. 独立使用场景

| 场景 | 说明 | 输入 | 输出 |
|------|------|------|------|
| **快速生成接口文档** | 根据代码和截图生成文档 | 接口代码、UI 截图 | MD 格式接口文档 |
| **增量更新文档** | 在现有文档中追加新接口 | 新接口代码、现有文档 | 更新后的文档 |
| **生成 Mock 数据** | 仅生成模拟数据 | 表结构或接口定义 | Mock JSON 数据 |
| **生成 OpenAPI** | 仅生成 OpenAPI 规范 | 表结构或接口定义 | OpenAPI YAML |

## 9. 注意事项

1. **输入格式**：确保表结构和 UI Element Tree 格式正确
2. **图片处理**：UI 截图会保存到文档目录下的 images 文件夹
3. **增量更新**：相同路径的接口会自动更新，不会重复添加
4. **文档迁移**：生成的 MD 文档格式统一，便于拷贝到其他地方